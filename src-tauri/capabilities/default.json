{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. The app's own commands are exposed via the invoke handler; no extra plugin permissions are needed for I-1.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
