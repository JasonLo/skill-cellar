{
  "$schema": "https://schema.tauri.app/config/2",
  "app": {
    "withGlobalTauri": true,
    "security": {
      "capabilities": [
        {
          "identifier": "mcp-bridge-dev",
          "description": "Dev-only: grants the MCP bridge plugin's commands to the main window so the Tauri MCP server can drive the app. Merged in only via `--config` from the tauri:dev script; never part of the production capability set (P-10).",
          "windows": ["main"],
          "permissions": ["mcp-bridge:default"]
        }
      ]
    }
  }
}
